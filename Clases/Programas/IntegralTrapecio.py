#!/usr/bin/python2.7
# -*- coding: utf-8 -*-

def intervalo(a,b):
    L = []
    n = 4
    dx = (b-a)/n
    while dx <= a:
        dx += dx
        f(x) = -6(dx)**3+5(dx)**2+2(dx)+12
        L.append(f(x))
        for i in L:
            i
    inte = (b-a/2n)(f
    
